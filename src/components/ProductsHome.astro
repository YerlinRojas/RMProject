---
import Data from "../../public/data/prodHome.json"
import CardProducts1 from "./CardProducts1.astro";
import CardProducts2 from "./CardProducts2.astro";

const data = Data
---
<div>
<div class="containerProductsHome border-t-4">
  
<h3 class="text-center  font-semibold mb-5 mt-2 text-4xl lg:text-6xl "> PRODUCTOS</h3>

{data.map((product, index) => {
    const esPar = index % 2 === 0;
    if (esPar) {
        return <CardProducts1 title={product.title} desc={product.desc} img={product.img} link={product.link}/>;
    }
        return <CardProducts2 title={product.title} desc={product.desc} img={product.img} link={product.link} />;
    })}

</div>
</div>

<style>
    .containerProductsHome{
        display: grid;
        grid-template-columns: auto;
        grid-template-rows: auto auto auto auto auto auto;
        
    }


    
</style>